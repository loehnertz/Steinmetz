matrix:
  include:
    - language: node_js
      before_script:
        - cd frontend
        - npm install
      script:
        - npm test
    - language: java
      jdk: openjdk11
      before_script:
        - cd backend
        - mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
      script:
        - mvn test -B
